<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="hsj.expmle.com">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:supportsRtl="true"

        >

        <activity android:name=".activity.AboutInfoActivity"
            android:theme="@style/user_mode_CustomTheme"
            ></activity>
        <activity android:name=".activity.PdaParamSettingActivity"
            android:theme="@style/user_mode_CustomTheme"
            >
        </activity>

        <activity android:name=".activity.UserInfoSettingActivity"
            android:theme="@style/user_mode_CustomTheme"
            ></activity>
    </application>

</manifest>
